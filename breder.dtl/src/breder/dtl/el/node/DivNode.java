package breder.dtl.el.node;

import breder.dtl.el.parse.Token;

public class DivNode extends ArithmeticNode {

	public DivNode(Token token, Node left, Node right) {
		super(token, left, right);
	}

}

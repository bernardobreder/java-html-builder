package breder.dtl.el.node;

import breder.dtl.el.parse.Token;

public class LowerNode extends ConditionNode {

	public LowerNode(Token token, Node left, Node right) {
		super(token, left, right);
	}

}

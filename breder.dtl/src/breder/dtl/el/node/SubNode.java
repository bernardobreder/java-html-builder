package breder.dtl.el.node;

import breder.dtl.el.parse.Token;

public class SubNode extends ArithmeticNode {

	public SubNode(Token token, Node left, Node right) {
		super(token, left, right);
	}

}
